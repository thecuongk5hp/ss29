
// tinhs s tam giác

// biết chiều cao (h) = 20, cạnh dáy (a)= 30




function triangleS(a, h) {
    //(a, h) -tham số
    //let a= 10;
    //let h= 20;
    let s = (a * h) / 2;

    console.log("Diện tích tam giác S=", s);
}

// sử dụng N lần
 triangleS(10,20); //-Đối số thực tế

// trong tương lai không xa 1
triangleS(20,25);

// trong tương lai không xa 2
triangleS(30,20);
